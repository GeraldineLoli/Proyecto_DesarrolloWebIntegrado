import { Component } from '@angular/core';
import { CommonModule } from '@angular/common';

@Component({ selector: 'app-login', standalone: true, imports: [CommonModule],
  template: `<p>Login — página del equipo (pendiente de pull)</p>` })
export class LoginComponent {}
